services:
  bot:
    build: .
    container_name: aski-su-kesintisi
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  bot-data:
